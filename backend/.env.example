PORT=5000
NODE_ENV=development
MONGODB_URI=mongodb://127.0.0.1:27017/vr_digital
JWT_SECRET=vr-digital-super-secret-change-me-in-production
JWT_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:5173,http://localhost:3000
# The API listens on all network interfaces so clients can use the server IP.

# Email OTP (use a verified sender with Resend, or configure SMTP)
RESEND_API_KEY=
RESEND_FROM="WakeelHub" <verified-email@yourdomain.com>
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=talhamehmoodpak0099@gmail.com
SMTP_PASS=hduu wxsy bjtr wemw
SMTP_FROM="WakeelHub" <your-email@gmail.com>

# Optional: enables the advanced LLM assistant; without this, the curated knowledge fallback is used.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
